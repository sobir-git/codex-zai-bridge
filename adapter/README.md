# open-responses-server (vendored)

This directory contains a vendored, lightly patched copy of the MIT-licensed
`open-responses-server` adapter used by `codex-zai`.

The packaged bridge uses it to translate OpenAI Responses API requests from
Codex into OpenAI-style chat completions requests that Z.ai accepts.
