# Publishing Checklist

Publish status:

This repo is already created at:

```text
https://github.com/sobir-git/codex-zai-bridge
```

Before announcing it:

1. Set your preferred copyright holder in [`LICENSE`](/home/fire/codex-zai-bridge/LICENSE) if you want something more specific.
2. Initialize git and commit:
   ```bash
   cd /home/fire/codex-zai-bridge
   git add .
   git commit -m "Initial release"
   ```
3. Push to GitHub:
   ```bash
   git push -u origin main
   ```

Recommended repo description:

```text
Run current Codex CLI with Z.ai GLM models through a local Responses API bridge.
```
